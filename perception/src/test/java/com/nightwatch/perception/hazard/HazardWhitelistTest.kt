package com.nightwatch.perception.hazard

import org.junit.Test

/**
 * Hazard whitelist + proximity tests (P5 acceptance).
 *
 * REQUIRED CONTENT:
 * - Raw COCO classes map to hazard labels correctly; non-hazards -> null.
 * - Proximity: overlapping boxes -> high score/touching; far boxes -> low score.
 * - Whitelist version is stable and additive.
 *
 * NOTE: pure geometry/unit tests; no camera needed.
 */
class HazardWhitelistTest {

    @Test
    fun `placeholder`() {
        // TODO: replace with whitelist mapping and proximity table tests.
    }
}