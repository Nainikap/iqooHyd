# NightWatch Contact (receiver) ProGuard/R8 rules.
# REQUIRED CONTENT:
# - Keep the payload deserialization classes and crypto providers.
# - Keep any reflection-based JSON parser models.

# TODO: add keeps for the chosen JSON/crypto libraries.